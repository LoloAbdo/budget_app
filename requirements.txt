# Pinned for reproducible release builds. Bump deliberately, then re-run the
# test suite and a real build before tagging a release.
PyQt6==6.11.0
matplotlib==3.10.9
pandas==2.2.2
python-dateutil==2.9.0.post0   # used by services/recurring_service.py (also a pandas/matplotlib dep)
bcrypt==5.0.0
beautifulsoup4==4.15.0          # HTML parsing for the Shopping price tracker
reportlab==4.5.1
openpyxl==3.1.5
pytest==8.2.2
pytest-cov==7.1.0
pytest-qt==4.5.0
